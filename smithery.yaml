startCommand:
  type: stdio
  configSchema: {}
  commandFunction: |
    (config) => ({
      "command": "node",
      "args": ["build/index.js"],
      "env": {}
    })
build:
  dockerfile: Dockerfile
  dockerBuildPath: .